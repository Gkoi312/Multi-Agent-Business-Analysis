# Backend Runtime

This directory is both the backend code root and the runtime root for the separated deployment.

Backend runtime data resolves relative to `backend/`:

- `backend/.runtime/`
- `backend/generated_report/`
- `backend/users.db`

The Python package now lives under `backend/app/`.
